import React from 'react'

const Custom = (props) => {
    const {countDown} = props

    return (
        <div>
            <button onClick = {custom}>Math Custom</button>
        </div>
    )
}

export default Custom